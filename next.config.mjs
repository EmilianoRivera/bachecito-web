/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com', 'firebasestorage.googleapis.com', 'i.postimg.cc'],
      },
};

export default nextConfig;
