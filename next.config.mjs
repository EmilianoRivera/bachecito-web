/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com', 'firebasestorage.googleapis.com', 'i.postimg.cc', 'i.postimage.cc'],
      },
    
};

export default nextConfig;
